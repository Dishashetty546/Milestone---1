//step 1: install all the dependencies like node.js - run time environment, express - framework of js. to get or build API'S
//body-parser- for interacting with datasets, INTERACTING WITH json files, middleware
//fs- filesystem, its a node.js module

//step 2: defining tge project structure
//setting up server. server running in port 30004

//step 3: apo to initialize express  application, port on which express is running 



testing 

-----------------------------------------------------
node index.js - server running
---------------------------------------------------
post - http://localhost:6000/articles on postman-url

post the article title, tags etc, body - raw, json type
                                  header - key- content-type
                                           value - application.jsos
--------------------------------------------------------------------

update - 
